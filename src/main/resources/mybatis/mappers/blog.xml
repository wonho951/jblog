<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="blog">

	<!-- 회원가입시 블로그 생성 -->
	<insert id="insertBlog" parameterType="BlogVo"> 	
		<![CDATA[
			insert into blog
			values (  #{id}
			         ,#{blogTitle}
			         ,#{logoFile}
			)
		]]>
	</insert>

	<!-- 자기 블로그 조회 -->
	<select id = "selectOne" parameterType="String" resultType="BlogVo">
		<![CDATA[
			select   b.id
			        ,b.blogTitle
			        ,b.logoFile
			        ,u.username
			from blog b, users u
			where b.id = u.id
			and b.id = #{id}
		]]>
	</select>
	
	
	<!-- 블로그 기본설정(타이틀, 이미지 바꾸기) -->
	<select id = "update" parameterType="BlogVo">
		<![CDATA[
			update blog
			set blogTitle = #{blogTitle},
			    logoFile = #{logoFile}
			where id = #{id}
		]]>
	</select>
</mapper>
